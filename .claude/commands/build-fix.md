# /build-fix
Usage: /build-fix
Diagnoses and fixes build failures automatically.
## Steps
1. Run build and capture errors
2. Parse error messages for root cause
3. Apply targeted fixes
4. Re-run build to verify
5. Repeat until build passes
