# /setup-pm
Usage: /setup-pm
Detects and configures the package manager for the project.
## Steps
1. Detect lock files (package-lock, pnpm-lock, yarn.lock, bun.lockb)
2. Identify the correct package manager
3. Configure scripts and aliases
4. Verify installation works
5. Report detected configuration
