# /claw
Usage: /claw <config-key> [value]
OpenClaw configuration management for Claude Code behaviors.
## Steps
1. Read current configuration
2. Set or display the requested config key
3. Validate the new value
4. Apply configuration change
5. Confirm the update
