# /instinct-export
Usage: /instinct-export [filepath]
Exports all learned instincts to a portable JSON file.
## Steps
1. Gather all instincts from storage
2. Serialize to JSON format
3. Include metadata (version, date, counts)
4. Write to specified filepath
5. Confirm export with statistics
