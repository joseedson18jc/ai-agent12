# Django TDD
factory_boy, pytest-django, request factories, API testing.
## Instructions
- Use factory_boy for model fixtures
- Test views with RequestFactory (fast) and Client (integration)
- Use pytest-django for database access markers
- Test API endpoints with DRF's APIClient
- Mock external services in tests
