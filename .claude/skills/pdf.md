# PDF Processing
Extract text, tables, metadata from PDFs with merge and annotation support.
## Instructions
- Use pdf-parse, pdfjs-dist, or pdftotext for extraction
- Preserve document structure (headings, tables, lists)
- Output tables as structured JSON/CSV
- Handle multi-page with page references
- Preserve metadata (author, title, dates)
