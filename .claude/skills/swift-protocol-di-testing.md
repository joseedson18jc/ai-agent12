# Swift Protocol DI Testing
Protocol-oriented dependency injection for testable architectures.
## Instructions
- Define protocols for all external dependencies
- Inject dependencies via initializer
- Create mock implementations for testing
- Use protocol extensions for default behavior
- Keep protocols focused (Interface Segregation)
