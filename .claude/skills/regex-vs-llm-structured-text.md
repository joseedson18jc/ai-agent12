# Regex vs LLM Structured Text
Decision framework: when to use regex vs LLM for text extraction.
## Instructions
- Use regex for: fixed formats, known patterns, high volume
- Use LLM for: variable formats, natural language, ambiguous structure
- Hybrid: regex for extraction, LLM for interpretation
- Benchmark both approaches on sample data
- Consider cost, latency, and accuracy tradeoffs
