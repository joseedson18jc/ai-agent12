# Backend Patterns
API design, middleware, caching, service layers.
## Instructions
- Separate routes, controllers, services, repositories
- Use middleware for auth, logging, rate limiting
- Cache expensive queries with TTL
- Validate inputs at the API boundary
- Return consistent response envelopes
