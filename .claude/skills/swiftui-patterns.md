# SwiftUI Patterns
View composition, state management, navigation, preview providers.
## Instructions
- Compose small, reusable views
- Use @State for local, @ObservedObject for shared state
- Implement NavigationStack for navigation
- Create preview providers with sample data
- Extract view modifiers for reuse
