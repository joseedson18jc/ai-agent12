# Swift Concurrency
Swift 6.2 async/await, structured concurrency, task groups, sendable.
## Instructions
- Use async/await for asynchronous operations
- Prefer structured concurrency (TaskGroup, async let)
- Mark types as Sendable for cross-actor safety
- Use actors for mutable shared state
- Handle cancellation with Task.checkCancellation()
