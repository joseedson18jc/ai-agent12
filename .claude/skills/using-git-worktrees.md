# Using Git Worktrees
Parallel development branches for efficient context switching.
## Instructions
- Use `git worktree add` for parallel branches
- Keep worktrees in a sibling directory
- Clean up with `git worktree remove` when done
- Share the same git objects (space efficient)
- Useful for reviewing PRs while working on features
